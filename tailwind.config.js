module.exports = {
  theme: {
    extend: {},
    backgroundColor: theme => ({
      screen: 'var(--bg-screen)',
      tile: 'var(--bg-tile)'
    })
    // fontSize: theme => ({
    //   'base': '1rem'
    // })
  },
  variants: {},
  plugins: []
}
